"""HTTP and WebSocket surface.

The FastAPI app is built by :func:`pitwall.api.main.create_app`. The
module-level ``app`` in :mod:`pitwall.api.main` is the entry point
for ``uvicorn pitwall.api.main:app``.
"""
