# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
env/
.python-version

# uv / poetry
.uv-cache/
poetry.lock

# Node
node_modules/
dist/
.vite/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Frontend build
frontend/dist/
frontend/.vite/

# Playwright
frontend/playwright-report/
frontend/test-results/
frontend/.playwright-libs/
frontend/libasound2t64*.deb

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yaml

# Env
.env
.env.local
.env.*.local

# FastF1 cache (puede crecer a varios GB)
data/cache/
data/raw/
data/processed/
data/ml/*.json
data/ml/*.parquet
reports/figures/
reports/ml/

data/ml/
# data/causal/ is gitignored by default because the parquet files are large
# and regenerable via `make build-causal-dataset`.
# The causal dataset files on main/causal2 were force-committed with `git add -f`
# during the causal model development sprint. After a fresh clone, run:
#   make db-up && make ingest-demo && make build-causal-dataset
# to regenerate them. See docs/causal_model_performance.md for details.
data/causal/

# Modelos serializados grandes (regenerables)
models/*.json
models/*.bin
!models/.gitkeep

# Notebook outputs
.ipynb_checkpoints/

# Claude / agent local state
.claude/settings.local.json

.DS_Store
